using UnityEngine;
using UnityEngine.InputSystem;
using Unity.Netcode;

namespace oren_Network
{
    public class OnlinePuzzleButton : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}

